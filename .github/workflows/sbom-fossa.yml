name: FOSSA + SBOM Workflow (feature/fossa-sbom)

on:
  push:
    branches: [ feature/fossa-sbom ]
  pull_request:
    branches: [ feature/fossa-sbom ]

jobs:
  fossa-sbom-pipeline:
    name: Generate SBOM & FOSSA Analysis
    runs-on: ubuntu-latest

    steps:

      # 1️⃣ Checkout repository
      # -----------------------------------------------------------
      - name: 🧩 Checkout source code
        uses: actions/checkout@v4

      # -----------------------------------------------------------
      # 2️⃣ Setup Node environment
      # -----------------------------------------------------------
      - name: ⚙️ Setup Node.js (v18)
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      # -----------------------------------------------------------
      # 3️⃣ Install dependencies
      # -----------------------------------------------------------
      - name: 📦 Install npm dependencies
        run: |
          echo "Installing dependencies..."
          npm ci
          echo "✅ Installation complete"

      # -----------------------------------------------------------
      # 4️⃣ Generate CycloneDX SBOM using Syft
      # -----------------------------------------------------------
      - name: 🧾 Generate SBOM with Anchore Syft
        run: |
          echo "🔍 Generating SBOM..."
          docker run --rm -v "${{ github.workspace }}":/project anchore/syft:latest dir:/project -o cyclonedx-json > sbom.cdx.json
          echo "✅ SBOM file created at $(pwd)/sbom.cdx.json"

      # -----------------------------------------------------------
      # 5️⃣ Upload SBOM artifact
      # -----------------------------------------------------------
      - name: 📤 Upload SBOM artifact to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: sbom-cyclonedx
          path: sbom.cdx.json

      # -----------------------------------------------------------
      # 6️⃣ Run FOSSA CLI Scan
      # -----------------------------------------------------------
      - name: 🔍 Run FOSSA dependency & license analysis
        uses: fossas/fossa-action@v1
        with:
          api-key: ${{ secrets.FOSSA_API_KEY }}

      # -----------------------------------------------------------
      # 7️⃣ Display SBOM summary (optional)
      # -----------------------------------------------------------
      - name: 📊 Display SBOM summary
        run: |
          echo "Top of SBOM file:"
          head -n 15 sbom.cdx.json
          echo "✅ SBOM validation done"
